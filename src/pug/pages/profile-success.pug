doctype html
html(lang="en")
    - var page_title = 'Профиль - проверьте почту'
    include ../head.pug 

    body.bg-dark
        - var template_main = 'profile-success'
        include ../layouts/leftsidebar.pug 
                
        include ../foot.pug 

        script.
            $(document).ready(function() {
                $('[data-id="sendmail"]').addClass("active");
            });